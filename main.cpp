#include <iostream>
#include "tree.h"
int main(){
    Tree<int> t;
    RedBlackTree<int> rb_tree;
    rb_tree.insert(1);
    rb_tree.insert(10);
    //...

    rb_tree.find(some);


    AVLTree avl_tree;

    Queue queue;
    PriorityQueue priority_queue;

    return 0;
}